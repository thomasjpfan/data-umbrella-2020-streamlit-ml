{
    "install": "pip3 install --user -U setuptools pip && pip3 install --no-cache-dir --user -r requirements.txt",
    "start": "PYTHONUNBUFFERED=true streamlit explain.py",
    "watch": {
        "throttle": 1000
    }
}
